<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/assets/styles.css" />
    <title>Yams</title>
    <script src="https://kit.fontawesome.com/07ca895015.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" integrity="undefined" crossorigin="anonymous">
</head>
<body class="bodyBackground">
    <div class="container ">
        <header>
            <%- include('../partials/header')%>
        </header>
        <div class="row">
            <div class="col-md">
               <h1>
                   <%= title %>
               </h1>
               <div>
                <% if(data.length)  { %>
                    <h2>
                        Tentez votre chance de remporter jusqu'à 3 de nos patisseries en cliquant sur les dés
                    </h2>
                <% } else { %>
                    <p>Désolé pas de contenu</p>
                <% } %>
               </div>
            </div>
            <div class="playDiv">
                <a href="/game" style="text-decoration: none;">
                    <i class="fas fa-dice" style="color: black;font-size:200px"></i>
                </a>
            </div>

            <h3>Pâtisseries encore disponibles : </h3>
            <ul>
                <% data.forEach(({name, number, order }) => { %>
                    <li><%= number %> <%= name %></li>
                <%  }) %>
            </ul>
            <footer>
                <%- include('../partials/footer')%>
            </footer>
        </div>
    </div>
</body>
</html>